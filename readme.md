# Monitrly Static template

This is the static template that is used for all projects on Monitrly. Built with Jigsaw.

All variables are stored in `config.php`, the values are inserted by the Monitrly API.
